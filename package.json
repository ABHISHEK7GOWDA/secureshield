{
  "name": "secureshield-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build && cd ../frontend && npm install --legacy-peer-deps && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist"
  }
}
